name: Sync PokeAPI api-data

on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Info
        run: |
          echo "PokeAPI data is intentionally excluded from git." 
          echo "Run 'npm run sync' locally (or on your VPS) to download it."
